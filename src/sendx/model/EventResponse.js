/**
 * SendX REST API
 * # Introduction SendX is an email marketing product. It helps you convert website visitors to customers, send them promotional emails, engage with them using drip sequences and craft custom journeys using powerful but simple automations. The SendX API is organized around REST. Our API has predictable resource-oriented URLs, accepts form-encoded request bodies, returns JSON-encoded responses, and uses standard HTTP response codes, authentication, and verbs. The SendX Rest API doesn’t support bulk updates. You can work on only one object per request. <br> 
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: support@sendx.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The EventResponse model module.
 * @module sendx/model/EventResponse
 * @version 1.0.0
 */
class EventResponse {
    /**
     * Constructs a new <code>EventResponse</code>.
     * @alias module:sendx/model/EventResponse
     */
    constructor() { 
        
        EventResponse.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>EventResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:sendx/model/EventResponse} obj Optional instance to populate.
     * @return {module:sendx/model/EventResponse} The populated <code>EventResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new EventResponse();

            if (data.hasOwnProperty('event_id')) {
                obj['event_id'] = ApiClient.convertToType(data['event_id'], 'String');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = ApiClient.convertToType(data['status'], 'String');
            }
            if (data.hasOwnProperty('message')) {
                obj['message'] = ApiClient.convertToType(data['message'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>EventResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>EventResponse</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['event_id'] && !(typeof data['event_id'] === 'string' || data['event_id'] instanceof String)) {
            throw new Error("Expected the field `event_id` to be a primitive type in the JSON string but got " + data['event_id']);
        }
        // ensure the json data is a string
        if (data['status'] && !(typeof data['status'] === 'string' || data['status'] instanceof String)) {
            throw new Error("Expected the field `status` to be a primitive type in the JSON string but got " + data['status']);
        }
        // ensure the json data is a string
        if (data['message'] && !(typeof data['message'] === 'string' || data['message'] instanceof String)) {
            throw new Error("Expected the field `message` to be a primitive type in the JSON string but got " + data['message']);
        }

        return true;
    }

/**
     * Returns Unique identifier for the created event.
     * @return {String}
     */
    getEventId() {
        return this.event_id;
    }

    /**
     * Sets Unique identifier for the created event.
     * @param {String} eventId Unique identifier for the created event.
     */
    setEventId(eventId) {
        this['event_id'] = eventId;
    }
/**
     * Returns Status of the event creation (e.g., 'success').
     * @return {String}
     */
    getStatus() {
        return this.status;
    }

    /**
     * Sets Status of the event creation (e.g., 'success').
     * @param {String} status Status of the event creation (e.g., 'success').
     */
    setStatus(status) {
        this['status'] = status;
    }
/**
     * Returns Additional message about the event creation.
     * @return {String}
     */
    getMessage() {
        return this.message;
    }

    /**
     * Sets Additional message about the event creation.
     * @param {String} message Additional message about the event creation.
     */
    setMessage(message) {
        this['message'] = message;
    }

}



/**
 * Unique identifier for the created event.
 * @member {String} event_id
 */
EventResponse.prototype['event_id'] = undefined;

/**
 * Status of the event creation (e.g., 'success').
 * @member {String} status
 */
EventResponse.prototype['status'] = undefined;

/**
 * Additional message about the event creation.
 * @member {String} message
 */
EventResponse.prototype['message'] = undefined;






export default EventResponse;

