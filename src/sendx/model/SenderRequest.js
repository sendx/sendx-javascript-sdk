/**
 * SendX REST API
 * # Introduction The SendX API is organized around REST. Our API has predictable resource-oriented URLs, accepts form-encoded request bodies, returns JSON-encoded responses, and uses standard HTTP response codes, authentication, and verbs. The SendX Rest API doesn’t support bulk updates. You can work on only one object per request. <br> 
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: support@sendx.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The SenderRequest model module.
 * @module sendx/model/SenderRequest
 * @version 1.0.0
 */
class SenderRequest {
    /**
     * Constructs a new <code>SenderRequest</code>.
     * @alias module:sendx/model/SenderRequest
     * @param name {String} Name of the sender
     * @param email {String} Email address of the sender
     */
    constructor(name, email) { 
        
        SenderRequest.initialize(this, name, email);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, name, email) { 
        obj['name'] = name;
        obj['email'] = email;
    }

    /**
     * Constructs a <code>SenderRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:sendx/model/SenderRequest} obj Optional instance to populate.
     * @return {module:sendx/model/SenderRequest} The populated <code>SenderRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SenderRequest();

            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('email')) {
                obj['email'] = ApiClient.convertToType(data['email'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SenderRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SenderRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of SenderRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['email'] && !(typeof data['email'] === 'string' || data['email'] instanceof String)) {
            throw new Error("Expected the field `email` to be a primitive type in the JSON string but got " + data['email']);
        }

        return true;
    }

/**
     * Returns Name of the sender
     * @return {String}
     */
    getName() {
        return this.name;
    }

    /**
     * Sets Name of the sender
     * @param {String} name Name of the sender
     */
    setName(name) {
        this['name'] = name;
    }
/**
     * Returns Email address of the sender
     * @return {String}
     */
    getEmail() {
        return this.email;
    }

    /**
     * Sets Email address of the sender
     * @param {String} email Email address of the sender
     */
    setEmail(email) {
        this['email'] = email;
    }

}

SenderRequest.RequiredProperties = ["name", "email"];

/**
 * Name of the sender
 * @member {String} name
 */
SenderRequest.prototype['name'] = undefined;

/**
 * Email address of the sender
 * @member {String} email
 */
SenderRequest.prototype['email'] = undefined;






export default SenderRequest;

