/**
 * SendX REST API
 * # Introduction SendX is an email marketing product. It helps you convert website visitors to customers, send them promotional emails, engage with them using drip sequences and craft custom journeys using powerful but simple automations. The SendX API is organized around REST. Our API has predictable resource-oriented URLs, accepts form-encoded request bodies, returns JSON-encoded responses, and uses standard HTTP response codes, authentication, and verbs. The SendX Rest API doesn’t support bulk updates. You can work on only one object per request. <br> 
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: support@sendx.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The SenderResponse model module.
 * @module sendx/model/SenderResponse
 * @version 1.0.0
 */
class SenderResponse {
    /**
     * Constructs a new <code>SenderResponse</code>.
     * @alias module:sendx/model/SenderResponse
     * @param id {String} Encrypted ID of the sender
     * @param name {String} Name of the sender
     * @param email {String} Email address of the sender
     * @param isWhitelisted {Boolean} Indicates if the sender is whitelisted
     */
    constructor(id, name, email, isWhitelisted) { 
        
        SenderResponse.initialize(this, id, name, email, isWhitelisted);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, id, name, email, isWhitelisted) { 
        obj['id'] = id;
        obj['name'] = name;
        obj['email'] = email;
        obj['isWhitelisted'] = isWhitelisted;
    }

    /**
     * Constructs a <code>SenderResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:sendx/model/SenderResponse} obj Optional instance to populate.
     * @return {module:sendx/model/SenderResponse} The populated <code>SenderResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SenderResponse();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('email')) {
                obj['email'] = ApiClient.convertToType(data['email'], 'String');
            }
            if (data.hasOwnProperty('isWhitelisted')) {
                obj['isWhitelisted'] = ApiClient.convertToType(data['isWhitelisted'], 'Boolean');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SenderResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SenderResponse</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of SenderResponse.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['email'] && !(typeof data['email'] === 'string' || data['email'] instanceof String)) {
            throw new Error("Expected the field `email` to be a primitive type in the JSON string but got " + data['email']);
        }

        return true;
    }

/**
     * Returns Encrypted ID of the sender
     * @return {String}
     */
    getId() {
        return this.id;
    }

    /**
     * Sets Encrypted ID of the sender
     * @param {String} id Encrypted ID of the sender
     */
    setId(id) {
        this['id'] = id;
    }
/**
     * Returns Name of the sender
     * @return {String}
     */
    getName() {
        return this.name;
    }

    /**
     * Sets Name of the sender
     * @param {String} name Name of the sender
     */
    setName(name) {
        this['name'] = name;
    }
/**
     * Returns Email address of the sender
     * @return {String}
     */
    getEmail() {
        return this.email;
    }

    /**
     * Sets Email address of the sender
     * @param {String} email Email address of the sender
     */
    setEmail(email) {
        this['email'] = email;
    }
/**
     * Returns Indicates if the sender is whitelisted
     * @return {Boolean}
     */
    getIsWhitelisted() {
        return this.isWhitelisted;
    }

    /**
     * Sets Indicates if the sender is whitelisted
     * @param {Boolean} isWhitelisted Indicates if the sender is whitelisted
     */
    setIsWhitelisted(isWhitelisted) {
        this['isWhitelisted'] = isWhitelisted;
    }

}

SenderResponse.RequiredProperties = ["id", "name", "email", "isWhitelisted"];

/**
 * Encrypted ID of the sender
 * @member {String} id
 */
SenderResponse.prototype['id'] = undefined;

/**
 * Name of the sender
 * @member {String} name
 */
SenderResponse.prototype['name'] = undefined;

/**
 * Email address of the sender
 * @member {String} email
 */
SenderResponse.prototype['email'] = undefined;

/**
 * Indicates if the sender is whitelisted
 * @member {Boolean} isWhitelisted
 */
SenderResponse.prototype['isWhitelisted'] = undefined;






export default SenderResponse;

